[
    { type: "settings", position: "label-left", labelWidth: 80, inputWidth: 450 },
    {    type:"fieldset",
         name: "connection",
         label:"{{ "New"| trans}}",
         width:600,
		 list: [ 
		{ type: "input",
                   name: "scheduler",
                   label: "{{ "scheduler"| trans }}"
        },

		{ type: "input",
                   name: "cluster_options",
                   label: "{{ "cluster_options"| trans }}"
        },

		{ type: "input",
                   name: "licence",
                   label: "{{ "licence"| trans }}"
        },

		{ type: "input",
                   name: "install_path",
                   label: "{{ "install_path"| trans }}"
        },

		{ type: "input",
                   name: "user_path",
                   label: "{{ "user_path"| trans }}"
        },

		{ type: "input",
                   name: "events",
                   label: "{{ "events"| trans }}"
        },

		{ type: "input",
                   name: "os_target",
                   label: "{{ "os_target"| trans }}"
        },

		{ type: "input",
                   name: "version",
                   label: "{{ "version"| trans }}"
        },

		{ type: "input",
                   name: "install_date",
                   label: "{{ "install_date"| trans }}"
        },

		{ type: "input",
                   name: "status",
                   label: "{{ "status"| trans }}"
        },

		{ type: "input",
                   name: "status_date",
                   label: "{{ "status_date"| trans }}"
        },
]
    }
]
